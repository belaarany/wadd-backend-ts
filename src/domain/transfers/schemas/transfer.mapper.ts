import { Transfer } from "../interfaces/transfer.model"
import { TransferEntity } from "./transfer.entity"

export class TransferMapper {
  /*static fromEntity(document: TransferEntity): Transfer {
    return {
      id: document.id,
      kind: "transfer",
      source_wallet_id: document.source_wallet_id,
      source_amount: document.source_amount,
      source_currency: document.source_currency,
      target_wallet_id: document.target_wallet_id,
      target_amount: document.target_amount,
      target_currency: document.target_currency,
      timestamp: document.timestamp,
      note: document.note,
      is_deleted: document.is_deleted,
      created_at: document.created_at,
      updated_at: document.updated_at,
      deleted_at: document.deleted_at,
    }
  }*/
}
